#q = dmf.CircleEntity().get_entity()
#print(q.get_entity())

#print(dmf.create_9_circle_array())

#print(dmf.is_circle(q))
#data_model = q.get_entity()
#print(dmf.is_closed(data_model))

# entity = dmf.draw_circle(q)
# dmf.shrink(entity)

# entity = dmf.test_square_21
# dmf.expand(entity)

# entity = dmf.bmb_square_21
# center_point = dmf.get_center_point(entity)
# point2 = (center_point[0]+1,center_point[1]+2)
# dmf.connect_point(entity,point2,center_point,1)

#dmf.draw_rectangle(entity,(center_point[0],center_point[1]),15,15,True)

#print(entity)